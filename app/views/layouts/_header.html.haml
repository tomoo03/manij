%header
  .header-items
    -# .header-items__links--logo
    -#   = link_to 'Home', root_path
    .header-items__links
      - if user_signed_in?
        %button
          = link_to goals_path do
            = icon('fa', 'house-user')
            Home
      - else
        = link_to new_user_session_path do
          = icon('fa', 'user-lock')
          Log in
    - if user_signed_in?
      .header-items__links--box
        %button#header__btn
          = icon('fa', 'user-friends')
          Team
        #header__panel
          .header-items__links__menu
            -if user_signed_in?
              = link_to 'チーム作成', new_team_path(current_user)
            .header-items__links__menu
              = link_to 'チーム一覧', teams_path
            .header-items__links__menu
              = link_to 'Projects', projects_path
      .header-items__links--box
        %button#header__btn2
          = icon('fa','user-circle')
          Account
        #header__panel2
          .header-items__links__menu
            -if user_signed_in?
              = link_to 'ログアウト', destroy_user_session_path, method: :delete
    -# .header-items__links
    -# - if user_signed_in?
    -#   = link_to 'ログアウト', destroy_user_session_path, method: :delete
    -# .header-items__account
    -#   %ul.header-items__account__gnav
    -#     %li
    -#       = link_to 'Account', '#'
    -#       - if user_signed_in?
    -#         %ul
    -#           %li
    -#             = link_to 'Dashboard', '#'
    -#           %li
    -#             = link_to '目標の新規作成', new_goal_path
    -#           %li
    -#             = link_to 'Log out', destroy_user_session_path, method: :delete
    -#       - else
    -#         %ul
    -#           %li
    -#             = link_to 'Log in', new_user_session_path